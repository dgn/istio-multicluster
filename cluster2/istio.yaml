apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  meshConfig:
    accessLogFile: /dev/stdout
    outboundTrafficPolicy:
      mode: REGISTRY_ONLY
    trustDomain: cluster2.local
    trustDomainAliases:
      - cluster1.local
    defaultConfig:
      proxyMetadata:
        ISTIO_META_DNS_CAPTURE: "true"
  values:
    global:
      multiCluster:
        clusterName: cluster2
      network: network2
      imagePullPolicy: Always
      proxy:
        clusterDomain: cluster2.local
    pilot:
      image: registry.gitlab.com/dgrimm/istio/pilot:dgn
      env:
        NETWORK_NAME: network2
        PILOT_REMOTE_FEDERATION_DISCOVERY: "http://istio-egressgateway.istio-system.svc.cluster.local:8188"
